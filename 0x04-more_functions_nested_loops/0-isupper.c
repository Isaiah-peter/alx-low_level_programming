#include <stdio.h>
#include <ctype.h>

/**
 * _isupper - prototype function
 * @c pint int
 * Return: 1 or 0
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1)
}
else
{
return (0)	
}
return (0)
}
